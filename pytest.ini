[pytest]
testpaths = tests
markers =
    external_data: requires downloaded external libraries under g3_libraries_root (plan WP-G3R-2); skipped when absent
    slow: ejecuta un notebook de análisis entero contra un run real (~1.5 min cada uno); se salta con -m "not slow"
